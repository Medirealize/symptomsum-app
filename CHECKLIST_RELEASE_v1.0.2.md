# v1.0.2 リリース前チェックリスト

## 1) セキュリティ・設定
- [ ] `.env.local` がGit管理対象外になっている（`.gitignore`確認）
- [ ] `.env.example` に実キーが含まれていない
- [ ] Supabaseキー・Geminiキーが本番用/検証用で分離されている

## 2) オンボーディング動作
- [ ] `/onboarding/diagnosis` で `3/3` 完了後に戻されず次へ進む
- [ ] `/onboarding/future` で `target_years` / `future_title` 入力後に次へ進む
- [ ] `/onboarding/core` 保存後に `/dashboard` へ遷移する
- [ ] 途中リロードしてもフローが破綻しない

## 3) 日記入力・保存
- [ ] モード（禅/ライバル/秘書）が選択できる
- [ ] `一時保存` 後、再訪時に内容とモードが復元される
- [ ] 保存後に再表示しても内容が保持される
- [ ] 日付切替時に別日の内容を誤表示しない

## 4) Gemini連携
- [ ] 正常系でAI応答が返る
- [ ] クォータ超過時に分かりやすいエラー表示になる
- [ ] APIキー無効時に適切なエラー表示になる
- [ ] 404時のフォールバックが想定通り動く

## 5) UI/UX・Hydration
- [ ] 初回表示でHydrationエラーが出ない
- [ ] ローディング表示が不自然でない
- [ ] モバイル幅で表示崩れがない
- [ ] 日本語文言に不自然な箇所がない

## 6) 品質ゲート
- [ ] `npm run build` が成功する
- [ ] 主要ページを手動スモークテスト済み
- [ ] 不要ファイル/重複設定ファイルが混入していない
- [ ] リリースノート更新済み
